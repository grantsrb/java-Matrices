apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = "m"
version = '1.0'
mainClassName = "App"

repositories {
 mavenCentral()
}

dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.+'
}
